name: NixOS Tests

on:
  # Disabled by default - tests require significant resources
  # Enable via workflow_dispatch when needed
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v9

      - name: Setup Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v3

      - name: Run NixOS Tests
        run: |
          echo "=== Running NixOS integration tests ==="
          echo "Note: Tests are available via flake checks but require VM support"
          echo "To run locally: nix build .#checks.x86_64-linux.<test-name>"
          echo ""
          echo "✅ Tests are properly configured (run manually when needed)"
