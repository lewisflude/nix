# SSH keys and configuration
~/.ssh/
~/.ssh/id_*
~/.ssh/known_hosts
~/.ssh/config

# GPG keys and configuration
~/.gnupg/
~/.gnupg/*.key
~/.gnupg/*.gpg
~/.gnupg/private-keys-v1.d/
~/.gnupg/openpgp-revocs.d/
~/.gnupg/S.gpg-agent*
~/.gnupg/gpg-agent.conf

# API keys and tokens
*.key
*.pem
*.p12
*.pfx
*.crt
*.csr
openai-key
api-key
access-token

# Environment files with secrets
.env
.env.local
.env.*.local
# .envrc is committed as template
.envrc.local

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Build results
result
result-*

# Temporary files
*.tmp
*.bak
*.swp
*.swo
*~
secrets/secrets.yaml.temp

# Log files
*.log
logs/
# Explicitly ignore env dumps that may carry secrets
env_terminal.log

# Cache directories
.cache/
.nix/

# Backup files
*.backup
backup-*

# Old configuration directory - reference only
old-nixos-config/

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit (auto-generated by nix)
.pre-commit-config.yaml

# Keep these DX files (explicitly not ignored):
# .gitmessage
# .editorconfig
# .markdownlint.json

# nx
.nx/
nix.conf

# Cursor MCP (managed by home-manager, project-specific is committed)
# .cursor/mcp.json - DO NOT ignore, this is project-specific
