# Backward compatibility shim - redirects to new modular structure
# This file is kept for compatibility with existing imports
# New code should import modules/shared/features/theming/applications/editors/zed/default.nix directly
import ./zed/default.nix
