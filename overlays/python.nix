# Python overlay using nixpkgs-python
# DISABLED: nixpkgs-python packages have incompatible structure (missing 'dependencies' attribute)
# Standard nixpkgs packages are well-cached in nix-community cache (priority=1)
_final: _prev: {
  # Overlay disabled - using standard nixpkgs Python packages
}
