_: {
  # Disabled: services.yubikey-touch-detector not available in home-manager
  # services.yubikey-touch-detector = {
  #   enable = true;
  #   package = pkgs.yubikey-touch-detector;
  #   socket.enable = true;
  #   extraArgs = ["--libnotify"];
  # };
}
